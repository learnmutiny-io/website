import React from 'react'
import styles from "../style"

const Blog = () => {
  return (
    <div  className='h-screen'>
        <p className={`${styles.paragraph} mr-5 mt-8`}>Blogs to build</p>
    </div>
  )
}

export default Blog